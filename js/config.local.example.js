// 1. Copy this file to js/config.local.js (same folder).
// 2. Replace the placeholder below with your own free OpenWeatherMap
//    API key: https://openweathermap.org/api
//
// js/config.local.js is listed in .gitignore and must never be
// committed. Note that a key shipped to a static frontend is still
// publicly visible to anyone inspecting network requests in the
// browser — this file only keeps it out of source control, it does
// not make the key secret. For real deployments, restrict the key on
// the OpenWeatherMap dashboard (HTTP referrer / rate limits).
export const API_KEY = 'YOUR_OPENWEATHERMAP_API_KEY';
